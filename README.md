# InfraSlowRhythmLiveDetector

To do list:
- make MATLAB function to read text files saved by .py
- make MATLAB function to clean text files saved by .py, removing any timestamp after recording end
- make RAEDME with dependencies
- import doc from Nathan's google doc
